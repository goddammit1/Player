// Базовый smoke-тест. Полноценные тесты можно добавить позже —
// например, мокать TrackSource и проверять поведение PlayerService.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder test', () {
    expect(1 + 1, 2);
  });
}
